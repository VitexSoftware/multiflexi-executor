; Redirect PHP errors to stderr so that Symfony Process captures them
; and the executor can store them in the job's stderr column in the database.
; Without this, PHP Fatal Errors go only to syslog and are invisible to the executor.
error_log = /dev/stderr
